{
  "explorer.fileNesting.enabled": true,
  "explorer.fileNesting.expand": false,
  "explorer.fileNesting.patterns": {
    "README.md": "CHANGELOG*, HISTORY*, LICENSE*, LICENCE*, CONTRIBUTING*, CODE_OF_CONDUCT*, CODEOWNERS, AUTHORS, SECURITY*",
    "package.json": "pnpm-*, package-lock.json, yarn.lock, bun.lock*, .npmrc, .nvmrc, .node-version, commitlint.config.*, lefthook.yml, .commitlintrc*, .pnpmfile.cjs",
    "Cargo.toml": "Cargo.lock, rust-toolchain*, .rustfmt.toml, rustfmt.toml, clippy.toml, .clippy.toml, deny.toml, audit.toml, .cargo, cross.toml, justfile, Justfile",
    ".gitignore": ".gitattributes, .gitmodules, .gitmessage, .mailmap",
    "tsconfig.json": "tsconfig.*.json, jsconfig.*.json",
    ".env.example": ".env, .env.*"
  }{% if options["chinese_programming"] and 'rust' in layers %},
  "rust-analyzer.cargo.checkOnSave.command": "clippy"
{% endif %}
}