name = "Rust 工作区"
description = "Rust 多 Crate 工作区：工作区 Cargo.toml、clippy/rustfmt 统一规范、sccache/lld 优化、工作区 justfile"
depends = ["common"]
update_ignore = ["Cargo.toml", "justfile", "rust-toolchain.toml"]

