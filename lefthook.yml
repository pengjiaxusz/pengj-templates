# lefthook 配置：pre-commit + commit-msg（约定式提交校验）
pre-commit:
  parallel: true
  commands:
    rustfmt:
      glob: "*.rs"
      run: cargo fmt -- --check

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit "{1}"
